using UnityEngine;

/// <summary>
/// Joystick must not be a fixed position.
/// Therefore, We designed the joystick's position flexible.
/// That's so inspired idea.
/// </summary>
public class ModernJoystick : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	private void Update () {
		
	}
}
